# ============================================
# .env.docker — Used by docker-compose on EC2
# Copy this to your EC2 server alongside docker-compose.yml
# ============================================

# Docker Hub Image (change to your Docker Hub username/repo)
DOCKER_IMAGE=harsh/autonomous-ai-agent:latest

# Database Configuration (PostgreSQL container)
DB_USER=postgres
DB_PASSWORD=Harsh@213
DB_NAME=ai_agent_db

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-min-32-characters
JWT_EXPIRES_IN=7d

# Gemini AI
GEMINI_API_KEY=AIzaSyCsE27RhTPWAo0MJax5GymiQZLDdclrH3s
GEMINI_MODEL=gemini-2.0-flash

# Blockchain Configuration
POLYGON_RPC_URL=https://rpc-amoy.polygon.technology
CHAIN_ID=80002
CONTRACT_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3

# Wallet Security (32-byte hex key for AES-256 encryption)
WALLET_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Agent Defaults
DEFAULT_SPENDING_LIMIT=0.1

# Smart Contract Deployment
DEPLOYER_PRIVATE_KEY=

# CORS - Set to your Vercel frontend URL (e.g. https://your-app.vercel.app)
# Use * to allow all origins during development
CORS_ORIGIN=*
